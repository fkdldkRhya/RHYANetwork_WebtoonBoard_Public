export interface TicketType {
  webtoonId: number,
  provider: string,
  title: string,
  synopsis: string,
  articleStart: number,
  articleEnd: number,
  commentTarget: string,
  maxType: string,
  maxSize: number,
  isAccess: number
}
